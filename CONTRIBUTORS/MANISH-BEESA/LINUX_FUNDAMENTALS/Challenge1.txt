Flag :  
    geek{grep_is_good_to_find_things_cEDf1591}

Command Used :
    grep -o "geek{[^}]*}" challenge1.txt

proof : 
    https://drive.google.com/file/d/1tX49Bu5td7AJj2moqhi4LBeXjAQSDPk-/view?usp=sharing

Description about command used :
        grep it is used to find pattrens in a text file

        -o tells grep to print only the part of the line that matches the pattrens

        geek{ it matches the starting of the flag 

        [^}]* matches all characters until a closing } found

        } marks the end of the flag